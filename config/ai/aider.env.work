# Aider Environment Configuration - Work
# Place this file in your home directory as .env

# API Keys for LLMs - Company is covering the costs
OPENAI_API_KEY=your_work_openai_api_key_here
# Using only OpenAI models for work

# Editor Settings
AIDER_EDITOR=cursor --wait

# Custom Git Settings

# Custom Output Settings
AIDER_CHAT_HISTORY_FILE=.aider.chat.history.md
AIDER_CODE_THEME=monokai

# Chat Settings
AIDER_STREAM=true
AIDER_FANCY_INPUT=true

# Model Settings
# Default to GPT-4o since company is paying
AIDER_MODEL=gpt-4o 